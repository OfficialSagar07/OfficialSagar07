#include <stdio.h>
int main(){
    int a,b;
    printf("Enter the   value\n");
    scanf("%d%d",&a,&b);
     if (a<b)
    { printf(" The smallest value is: %d \n",a);}
    else{ printf("The smallest number :%d\n",b);
        
    }
    return 0;
    
}
